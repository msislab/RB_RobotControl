# Optional deps for stereo depth (Fast-FoundationStereo).
# PyTorch CUDA: install a wheel that matches your GPU, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# ONNX backend (camera.stereo_depth.backend: onnx):
# Use a CUDA 12.x–compatible ORT GPU wheel. Latest 1.27+ needs CUDA 13 and will
# fail to import (libcudart.so.13) on CUDA 12 / torch cu12x stacks.
onnxruntime-gpu==1.22.0
